# Changelog — Align

## 2026-06-28
- Added the product-design deep-dive workspace to the existing Align repo:
  `DESIGN.md`, three-tier design tokens (`design-tokens.json` + `tokens/`),
  `design-system/` docs, `docs/` (brief, PRD, ADR log, personas, IA, metrics,
  constraints, open questions), `.claude/rules/`, and `reference/`.
- Tokens derived from the live app palette in `artifacts/mobile/constants/colors.ts`.
- Cross-linked `CLAUDE.md` and `README.md` to the new workspace; corrected
  "Expo Router v3" → v6 in `CLAUDE.md` to match `package.json`.
