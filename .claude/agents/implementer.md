# Agent: Implementer

Turns approved designs into React Native + Expo Router (TypeScript) components in `src/components/`, strictly using
tokens and following `.claude/rules/code-style.md` and
`design-system/components.md`.
